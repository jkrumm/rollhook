{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "RollHook App Config",
  "description": "Per-app rollhook.yaml configuration",
  "type": "object",
  "required": ["name", "steps"],
  "properties": {
    "name": {
      "description": "Unique app name, matches the deploy/:app route param",
      "type": "string"
    },
    "compose_file": {
      "description": "Path to docker-compose file relative to clone_path, defaults to compose.yml",
      "default": "compose.yml",
      "type": "string"
    },
    "steps": {
      "description": "Ordered rollout steps — executed sequentially",
      "type": "array",
      "items": {
        "type": "object",
        "required": ["service"],
        "properties": {
          "service": {
            "description": "Docker Compose service name to roll out",
            "type": "string"
          }
        }
      }
    }
  }
}
